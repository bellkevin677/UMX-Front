import React from 'react';
import Card from '@material-ui/core/Card';

const Card = (props) => (
    <div style={{ color: '#fff' }}>
      <h2>Title</h2>
      <div>Date</div>
      <div>
        Info
      </div>
    </div>
  )

export default Card